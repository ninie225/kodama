<nav class="navbar navbar-expand-lg my-5 mx-5 py-2 rounded-4">
  <div class="container-fluid">

    <!-- Bouton mobile -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Logo mobile centré -->
    <a class="navbar-brand d-lg-none fw-bold mx-auto" href="{{ path('app_accueil') }}">
        KODAMA'S KITCHEN
    </a>

    <!-- Espace -->
    <div class="d-lg-none" style="width: 42px;"></div>

    <!-- Menu -->
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav w-100 d-lg-flex align-items-lg-center justify-content-lg-evenly gap-lg-5">

        <li class="nav-item">
          <a class="nav-link" href="{{ path('app_carte') }}">Carte</a>
        </li>

        <li class="nav-item">
          <a class="nav-link" href="{{ path('app_contact') }}">Contact</a>
        </li>

        <!-- Logo PC centré -->
        <li class="nav-item d-none d-lg-block">
          <a class="navbar-brand fw-bold mx-3" href="{{ path('app_accueil') }}">
            KODAMA'S KITCHEN
          </a>
        </li>

        <li class="nav-item">
          <a class="nav-link" href="{{ path('app_login') }}">Compte</a>
        </li>

        <li class="nav-item">
          <a class="nav-link" href="{{ path('app_panier') }}">
            Panier <span class="badge bg-dark text-white rounded-pill ms-1">{{ totalItems }}</span>
          </a>
        </li>

      </ul>
    </div>

  </div>
</nav>